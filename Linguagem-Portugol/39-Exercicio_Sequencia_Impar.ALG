Algoritmo "Exercicio Sequencia Impar"

Var

   x, i : inteiro

Inicio

      escreva("Digite o valor de x: ")
      leia(x)
      
      para i de 1 ate x faca // aqui eu faco para ele ler de 1 ate o numero digitado
           se i % 2 <> 0 entao // aqui é se o resto da divisao for diferente de 0, ele é impar
              escreval(i)
           fimse
      fimpara

Fimalgoritmo